# Secret Scan And Manual Secret Check

Date: 2026-06-07

Commands run:

```bash
git ls-files --cached --others --exclude-standard
git ls-files --cached --others --exclude-standard -z | xargs -0 rg -n "<private-key blocks, bearer tokens, API-key assignments, passworded DB URLs>"
git ls-files keys tb-hackathon-ssh.zip tb-hackathon-ssh keys.md .env .env.example
```

Result:

- No tracked or non-ignored private key files were found.
- `git ls-files keys tb-hackathon-ssh.zip tb-hackathon-ssh keys.md .env .env.example`
  returned only `.env.example` and `keys/.gitkeep`.
- Real `.env`, `keys/*`, `*.pem`, `*.key`, `keys.md`, `tb-hackathon-ssh/`, and
  `tb-hackathon-ssh.zip` are ignored by `.gitignore`.
- Matches from the scan were reviewed and classified as safe fixtures/placeholders:
  - backend tests intentionally include fake bearer tokens, fake passwords, and fake
    connection strings to prove redaction;
  - `.env.example` contains replacement placeholders and the local dev Postgres URL;
  - `.agents/skills/playwright-best-practices/infrastructure-ci-cd/docker.md` contains a
    generic documentation example database URL.

Manual UI/screenshot check:

- Desktop and mobile screenshots generated by `frontend/tests/ticket-states.spec.ts`
  were inspected.
- They show redacted or synthetic data only and do not include Phoenix tokens, SSH keys,
  `.env` values, or raw secret-like command output.
